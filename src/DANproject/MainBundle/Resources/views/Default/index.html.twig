{%  extends "::base.html.twig"%}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('bundles/danprojectmain/css/bootstrap.css') }}">
    <link rel="stylesheet" href="{{ asset('bundles/danprojectmain/css/custom.css') }}">
{% endblock %}

{% block body %}
    <div class="container">
        <table class="table table-hover">
            <tr>
                <th>nom</th>
                <th>prenom</th>
                <th>age</th>
            </tr>
            {% for element in users %}
            <tr>
                <td>{{ element.prenom }}</td>
                <td>{{ element.nom }}</td>
                <td>{{ element.age }}</td>
            </tr>
                {% endfor %}
        </table>
    </div>


{% endblock %}